
'use client'
import { fetchConversations } from '@/data/slices/userSlice'
import { useAppDispatch, useAppSelector } from '@/hooks/reduxHooks'
import React, { useEffect } from 'react'

export default function MainAppPage() {


    return (
        <div></div>
        // <div>{conversations?.map(conversation => <div key={conversation?.message} >{conversation?.message}</div>)}</div>
    )
}
